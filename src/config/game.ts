export const GAME_CONFIG = {
  animation: {
    spinDuration: 0.15,     // 更快的基础旋转速度
    spinInterval: 1500,     // 自动旋转间隔时间（毫秒）
    reelStagger: 0.08,      // 列延迟
    stopDuration: 800,      // 停止动画持续时间（毫秒）
  },
  rewards: {
    baseMultiplier: 1,
    matchMultiplier: 1.5,
  }
};